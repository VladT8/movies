import { ButtonComponent } from "./button";
import styled from "styled-components";
import { Theme, ThemeType } from "../../themeContext/themes";

interface ButtonComponentContainerProps {
  theme?: Theme;
  myTheme?: ThemeType;
}
